<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juhezi.coderslife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.juhezi.coderslife.function.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity android:name="com.juhezi.coderslife.function.add_log.AddLogActivity" />
        <activity android:name=".function.log_info.LogInfoActivity" />
        <activity android:name=".function.all_logs.AllLogsActivity" />
        <activity android:name=".function.draft_box.DraftBoxActivity" />
    </application>
</manifest>